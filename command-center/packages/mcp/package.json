{
  "name": "@command-center/mcp",
  "version": "1.0.0",
  "description": "MCP server and CLI for the command center tracker",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "command-center-mcp": "dist/index.js",
    "command-center": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@command-center/tracker-core": "*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.1"
  }
}
